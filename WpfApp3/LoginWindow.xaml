<Window x:Class="WpfApp3.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Username:" VerticalAlignment="Center" Margin="10"/>
        <TextBox x:Name="UsernameTextBox" Grid.Column="1" Margin="10"/>

        <TextBlock Text="Password:" VerticalAlignment="Center" Margin="10" Grid.Row="1"/>
        <PasswordBox x:Name="PasswordBox" Grid.Column="1" Margin="10" Grid.Row="1"/>

        <Button Content="Login" Grid.ColumnSpan="2" Margin="10" Grid.Row="2" Click="LoginButton_Click" HorizontalAlignment="Right" Width="80"/>
    </Grid>
</Window>
